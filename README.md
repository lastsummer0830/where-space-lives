# 공간이 머무는 곳 — 반응형 가구 · 인테리어 쇼핑몰

> 테마가 있는 공간을 제안하는 가구 · 인테리어 쇼핑몰 팀 프로젝트입니다.
> 상품을 나열하는 대신 **"우드", "다크", "코지", "네이처"처럼 공간 컨셉으로 큐레이션**해, 사용자가 원하는 분위기부터 고르게 했습니다.

`팀 4명` · `2026.02` · `HTML · CSS · JavaScript · jQuery`

<!-- TODO(캡처): 대표 화면 1장 + 스크롤 애니메이션 GIF 1개. 아래 "화면" 섹션 참조. -->

---

## 담당 역할 — 조장 · 서브 컨셉 페이지 · 상품 상세

팀 4명 중 **조장**을 맡아 협업 흐름을 설계하고, 화면에서는 **서브 컨셉 페이지와 상품 상세**를 전담했습니다. (메인 화면 · 헤더 · 로그인 · 상품 목록은 팀원이 담당했습니다.)

**협업 (조장으로서)**
- `feature` 브랜치 + Pull Request 워크플로우를 도입해, 팀원 각자가 독립 브랜치에서 작업하고 PR로 합치도록 운영했습니다.
- 총 **20여 개 feature 브랜치**와 **PR 리뷰 · 병합**으로 `main`을 관리해, 여러 명이 같은 정적 사이트를 만지면서도 충돌을 통제했습니다. (git 이력에 그대로 남아 있습니다.)

**화면 (직접 구현)**
- **컨셉 페이지 4종** — 우드(`햇살이 머무는 우드 컬렉션`) · 다크(`Dark comfort, clean design`) · 코지(`Cozy & Warm`) · 네이처(`자연을 닮은 휴식처`). 테마별로 상품을 묶어 보여주는 큐레이션 화면입니다.
- **상품 상세 페이지** — 가격 · **색상 선택** · 상세 이미지(매트리스 상세 6컷 등)로 구성한 단일 상품 화면.
- **스크롤 인터랙션** — 컨셉 페이지에서 스크롤에 따라 요소가 나타나는 애니메이션(`slide-left`, `fade-up`)을 직접 붙였습니다.
- **이미지 에셋** — 상품 · 컨셉 이미지 정리 및 `.avif` 최적화 적용.

---

## 주요 기능

| 구분 | 화면 | 내용 |
|---|---|---|
| 컨셉 큐레이션 | 서브 컨셉 페이지 (내 담당) | 공간 분위기(우드/다크/코지/네이처)별 상품 묶음 |
| 상품 상세 | 상세 페이지 (내 담당) | 가격 · 색상 옵션 선택 · 상세 이미지 |
| 상품 목록 | 메인 (팀원) | 카테고리 네비 · 상품 카드 렌더링 |
| 검색 · 헤더 | 메인 (팀원) | 헤더 네비게이션 · 검색 |
| 로그인 | 메인 (팀원) | 로그인 팝업 · 마이페이지 |

---

## 기술 스택

`HTML5` · `CSS3` · `JavaScript (ES Module)` · `jQuery` · `Font Awesome` · `Git / GitHub (PR 워크플로우)`

프레임워크 없이 **바닐라 JS + jQuery**로 구현했습니다. 상품 목록처럼 반복 렌더링이 필요한 곳은 ES Module로 스크립트를 분리했고, DOM 조작 · 이벤트가 잦은 UI(팝업 등)에는 jQuery를 썼습니다.

---

<!--
TODO(다음 세션 캡처): docs/images/에 3컷 넣기 — ① 네이처 컨셉 페이지 스크롤 애니메이션(subaj3.html) ② 상품 상세 색상 선택(detailPage1.html) ③ 우드 컨셉 페이지(SubProject.html). 정적 사이트라 file://로 열면 로드됨.
-->

## 트러블슈팅 — 스크롤 애니메이션이 화면마다 다르게 동작한 문제

- **상황**: 네이처 컨셉 페이지는 스크롤에 따라 요소가 나타나는 애니메이션(`animate-item` → `.show` 클래스 부여)으로 만들었는데, **내 화면에서는 특정 요소가 제때 안 나타나거나 어긋나게 보였습니다.**
- **파고든 지점**: 이 페이지는 `window`가 아니라 별도의 `.scroll-container`(세로 `scroll-snap`) 안에서 스크롤이 일어나는 구조라, 요소가 "화면에 들어왔는지" 판정하는 기준이 뷰포트 크기 · 스크롤 위치에 따라 달라졌습니다.
- **해결 과정**: 내 화면만으로는 재현 조건이 헷갈려서, **같은 코드가 팀원 화면에서는 어떻게 보이는지 직접 대조**했습니다. 화면 크기에 따라 증상이 갈리는 걸 확인하고, 애니메이션 트리거 기준과 대상 요소를 맞춰 어느 화면에서도 같게 나타나도록 수정했습니다.
- **배운 점**: 스크롤/뷰포트에 의존하는 UI는 **내 환경 하나로 판단하면 안 된다**는 것. 이후로는 화면 크기를 바꿔 보거나 다른 사람 환경에서 확인하는 습관이 생겼습니다.

---

## 회고

- 프레임워크 없이 바닐라로 만들며 DOM · 이벤트 · 스크롤 인터랙션의 기본기를 다졌습니다.
- 조장으로 PR 워크플로우를 운영한 경험이, 이후 팀 프로젝트(Popflix · KNOWVA)에서 협업 방식을 잡는 밑바탕이 됐습니다.
- 다시 만든다면: 컨셉 페이지의 반복되는 마크업을 컴포넌트화하고, 이미지 경로를 처음부터 상대경로로 통일하겠습니다.

---

<sub>가구 이미지 등 에셋은 팀 프로젝트 학습용으로 제작 · 수집한 것입니다.</sub>
